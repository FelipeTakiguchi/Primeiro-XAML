<Window x:Class="MeuPrograma.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MeuPrograma"
        mc:Ignorable="d"
        Title="MyApp" Height="920" Width="1080">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Label Grid.Column="1" Grid.Row="0">Oláaa</Label>
        <Label Grid.Column="1" Grid.Row="1">Mundoooo</Label>
        <ListBox Grid.Row="1" x:Name="lstName"/>

        <StackPanel Grid.Row="1" Grid.Column="1" Margin="2,0,2,0">
            <TextBox x:Name="txtName" Text="cxvvxc"/>
            <Button x:Name="btnAdd"  Margin="0,5,0,0" Click="ButtonAddName_Click">Add Name</Button>

        </StackPanel>
    </Grid>
</Window>
